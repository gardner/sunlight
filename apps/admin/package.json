{
  "name": "@sunlight/admin",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vinext dev",
    "build": "vinext build",
    "start": "vinext start",
    "deploy": "pnpm build && wrangler deploy dist/server/ssr/index.js --assets dist/client --config wrangler.jsonc"
  },
  "dependencies": {
    "radix-ui": "^1.4.3"
  }
}
